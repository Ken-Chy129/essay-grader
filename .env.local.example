# 复制为 .env.local 后填入真实值。本文件可入 git;.env.local 不可(已被 .gitignore 屏蔽)。

# —— 必填(缺一则启动即 log.Fatal)——
ANTHROPIC_BASE_URL=https://your-anthropic-gateway.example.com
ANTHROPIC_API_KEY=sk-xxx

# —— 可选(留空/删除则用代码内默认)——
MODEL=claude-opus-4-8
PORT=8787
DATA_DIR=data
APP_PASSWORD=          # 留空=不鉴权;填值=启用登录口令
